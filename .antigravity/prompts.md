# Trade Boom - Developer Prompts

### Rebuilding & Compiling
```
Run a local Next.js build compilation check to make sure the project compiles without any TypeScript or linting errors.
```

### Verifying Layout Responsiveness
```
Inspect the navigation and market-ribbon fixed placement on mobile viewports. Make sure there is no overlapping with env(safe-area-inset-top) status notches.
```

### Reviewing Form Submissions
```
Inspect the server actions file `src/actions/actions.ts` to make sure contacts and subscribers list structures match form schema constraints.
```
